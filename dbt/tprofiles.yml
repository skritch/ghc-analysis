harlem:
  outputs:
    local:
      type: postgres
      threads: 1
      host: localhost
      port: 5432
      user: harlem
      pass: harlem
      dbname: harlem
      schema: public

  target: local
